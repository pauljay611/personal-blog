import styled from 'styled-components';

const InfoContent = styled.div`
    font-size: 1.2em;
    line-height: 1.2em;
`
const TimeBlock = styled.div`
    display:　flex;
    justify-content:　space-between;
`
const List = styled.div`
    padding: 0 2%;
    font-size: 1.2em;
    line-height: 1.2em;
    list-style: outside;
`

export { InfoContent, TimeBlock, List }